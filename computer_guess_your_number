print("В этом проекте пользователь загадывает число от 1 до 1000, а компьютер попробует его угадать минимум за 10 попыток")
print("Если компьютер выдает число меньше вашего числа, вы пишите Больше, если больше, то пишите Больше, если компьютер угадал пишите Точно в цель")
def computer_guess_your_number(user_number): # пользователь пишет число, которое загадал, но компьютер его не видит
    num = 500
    print(num)
    i = num # счетчик, к которому компьютер будет прибавлять или уменьшать предполагаемое число
    string = input() # пользователь пишет угадал ли число компьютер
    while num != user_number:
        if string == "Меньше":
            i /= 2
            num -= i
            num = round(num)
            print(num)
            string = input()
        elif string == "Больше":
            i /= 2
            num += i
            num = round(num)
            print(num)
            string = input()
        else:
            print("Ура! Я старался)")
            break
